{# src/EDiff/Bundle/AdminBundle/Resources/views/Classe/show.html.twig #}

{% extends "EDiffAdminBundle::layout_admin.html.twig" %}

{% block title %}{{ parent() }} | Classe{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Fiche d'une classe</h1>
		
		{% if update %}
			<div class="alert alert-error">
				<button class="close" data-dismiss="alert" type="button">×</button>
				Votre classe a bien été modifiée !
			</div>
		{% endif %}
		
		<p>
			<label class="large" for="login">Libellé</label>{{ entity.libelle }}
		</p>
		
		</br>
		
		<p>
	        <a class="btn btn-primary btn" href="{{ path('classe') }}">
	            Retour à la liste
	        </a>

	        <a class="btn" href="{{ path('classe_edit', { 'id': entity.id }) }}">
	            Modifier
	        </a>

	        <form action="{{ path('classe_delete', { 'id': entity.id }) }}" method="post">
	            {{ form_widget(delete_form) }}
	            <button class="btn btn" type="submit">Supprimer cette classe</button>
	        </form>
		<p>

	</div>
{% endblock %}