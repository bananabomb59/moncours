{# src/EDiff/Bundle/AdminBundle/Resources/views/Competence/index.html.twig #}

{% extends layout %}

{% block title %}{{ parent() }} | Compétence{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Liste des compétences</h1>
		
		{% if delete %}
			<div class="alert alert-error">
				<button class="close" data-dismiss="alert" type="button">×</button>
				Votre compétence a bien été supprimée !
			</div>
		{% endif %}
		
		{% if entities|length > 0 %}
		<table class="table-bordered table">
		    <thead>
		        <tr>
		            <th>Libelle</th>
		            <th>Matière</th>
		            <th>Actions</th>
		        </tr>
		    </thead>
		    <tbody>
		    {% for entity in entities %}
		        <tr>
		            <td>{{ entity.libelle }}</td>
		            <td>{{ entity.matiere }}</td>
		            <td>	                
                        <a class="btn btn btn-small" href="{{ path('competence_show', { 'id': entity.id }) }}">Voir</a>   
                        <a class="btn btn btn-small" href="{{ path('competence_edit', { 'id': entity.id }) }}">Modifier</a>
		            </td>
		        </tr>
		    {% endfor %}
		    </tbody>
		</table>
		{% else %} 
		Aucune compétence </br>
		{% endif %}
		
		</br>
		
		<p>
	        <a class="btn btn-primary" href="{{ path('competence_new') }}">
	            Création d'une nouvelle compétence
	        </a>
		</p>

	</div>
{% endblock %}