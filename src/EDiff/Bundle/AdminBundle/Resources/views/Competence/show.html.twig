{# src/EDiff/Bundle/AdminBundle/Resources/views/Competence/show.html.twig #}

{% extends layout %}

{% block title %}{{ parent() }} | Compétence{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Fiche d'une compétence</h1>
		
		{% if update %}
			<div class="alert alert-error">
				<button class="close" data-dismiss="alert" type="button">×</button>
				Votre compétence a bien été modifiée !
			</div>
		{% endif %}
		
		<p>
			<label class="large" for="libelle">Libellé</label>{{ entity.libelle }}
		</p>
		<p>
			<label class="large" for="libelle">Matière</label>{{ entity.matiere }}
		</p>
		
		</br>
		
		<p>
	        <a class="btn btn-primary btn" href="{{ path('competence') }}">
	            Retour à la liste
	        </a>

	        <a class="btn" href="{{ path('competence_edit', { 'id': entity.id }) }}">
	            Modifier
	        </a>

	        <form action="{{ path('competence_delete', { 'id': entity.id }) }}" method="post">
	            {{ form_widget(delete_form) }}
	            <button class="btn btn" type="submit">Supprimer cette compétence</button>
	        </form>
		<p>

	</div>
{% endblock %}