{# src/EDiff/Bundle/AdminBundle/Resources/views/Reponse/new.html.twig #}

{% extends "EDiffAdminBundle::layout_admin.html.twig" %}

{% block title %}{{ parent() }} | Réponse{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Création d'une réponse</h1>
		
		<form action="{{ path('reponse_create') }}" method="post" {{ form_enctype(form) }}>
		    {{ form_widget(form) }}
		    <p>
		        <button class="btn btn-large" type="submit">Créer !</button>
		    </p>
		</form>
		
		</br>
		
		<p>
	        <a class="btn btn-primary btn" href="{{ path('question_show', { 'id': question.id }) }}">
	            Retour à la question
	        </a>
		<p>

	</div>
{% endblock %}