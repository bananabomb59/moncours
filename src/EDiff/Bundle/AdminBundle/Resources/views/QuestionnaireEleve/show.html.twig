<h1>QuestionnaireEleve</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Numero_question</th>
            <td>{{ entity.numeroquestion }}</td>
        </tr>
        <tr>
            <th>Reponse</th>
            <td>{{ entity.reponse }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('questionnaireeleve') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('questionnaireeleve_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('questionnaireeleve_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
