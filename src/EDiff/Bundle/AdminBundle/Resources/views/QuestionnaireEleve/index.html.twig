<h1>QuestionnaireEleve list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Numero_question</th>
            <th>Reponse</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('questionnaireeleve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.numeroquestion }}</td>
            <td>{{ entity.reponse }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('questionnaireeleve_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('questionnaireeleve_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('questionnaireeleve_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
