<h1>CompetenceEleveAnnee list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nb_points</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('competenceeleveannee_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nbpoints }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('competenceeleveannee_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('competenceeleveannee_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('competenceeleveannee_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
