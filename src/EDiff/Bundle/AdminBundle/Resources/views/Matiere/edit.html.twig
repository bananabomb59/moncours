{# src/EDiff/Bundle/AdminBundle/Resources/views/Matiere/edit.html.twig #}

{% extends "EDiffAdminBundle::layout_admin.html.twig" %}

{% block title %}{{ parent() }} | Matière{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Edition d'une matière</h1>
		
		<form action="{{ path('matiere_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
		    {{ form_widget(edit_form) }}
		    <p>
		        <button class="btn btn-large" type="submit">Modifier !</button>
		    </p>
		</form>
		
		</br>
		
		<p>
	        <a class="btn btn-primary" href="{{ path('matiere') }}">
	            Retour à la liste
	        </a>

	        <form action="{{ path('matiere_delete', { 'id': entity.id }) }}" method="post">
	            {{ form_widget(delete_form) }}
	            <button class="btn btn" type="submit">Supprimer cette matière</button>
	        </form>
		<p>

	</div>
{% endblock %}