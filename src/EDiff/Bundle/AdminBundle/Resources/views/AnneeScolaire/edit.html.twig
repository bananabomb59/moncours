{# src/EDiff/Bundle/AdminBundle/Resources/views/AnneeScolaire/edit.html.twig #}

{% extends "EDiffAdminBundle::layout_admin.html.twig" %}

{% block title %}{{ parent() }} | Année scolaire{% endblock %}

{% block body %}
    <div class="hero-unit utilisateur">
		<h1>Edition d'une année scolaire</h1>
		
		<form action="{{ path('anneescolaire_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
		    {{ form_widget(edit_form) }}
		    <p>
		        <button class="btn btn-large" type="submit">Modifier !</button>
		    </p>
		</form>
		
		</br>
		
		<p>
	        <a class="btn btn-primary" href="{{ path('anneescolaire') }}">
	            Retour à la liste
	        </a>

	        <form action="{{ path('anneescolaire_delete', { 'id': entity.id }) }}" method="post">
	            {{ form_widget(delete_form) }}
	            <button class="btn btn" type="submit">Supprimer cette année scolaire</button>
	        </form>
		<p>

	</div>
{% endblock %}