<h1>AnneeScolaire</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Annee_debut</th>
            <td>{{ entity.anneedebut }}</td>
        </tr>
        <tr>
            <th>Annee_fin</th>
            <td>{{ entity.anneefin }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('anneescolaire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anneescolaire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('anneescolaire_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
